import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  tinyLogo: {
    marginTop: 15,
    width: '70%',
    marginHorizontal: '15%',
    height: 90,
    resizeMode: "contain"
  },
});

export default styles;
